{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug GDB",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/test",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb", // adjust if needed (gdb.exe on Windows)
      "preLaunchTask": "build"
    }
  ]
}
